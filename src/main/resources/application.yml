spring:
  liquibase:
    change-log: classpath:db/changelog.xml
  datasource:
    url: jdbc:postgresql://localhost:5432/controle_estoque
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver